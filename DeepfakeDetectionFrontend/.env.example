# Base URL of the FastAPI backend. Defaults to http://127.0.0.1:8000 if unset.
# In production set this to your deployed backend (e.g. a Hugging Face Space URL).
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
